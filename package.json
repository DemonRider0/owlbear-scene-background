{
  "name": "owlbear-scene-background",
  "private": true,
  "version": "1.0.1",
  "description": "Cor de fundo da cena controlada pelo GM para o Owlbear Rodeo.",
  "author": "DemonRider",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner --host 127.0.0.1",
    "build": "tsc --noEmit && vite --configLoader runner build",
    "validate:build": "node scripts/validate-build.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@owlbear-rodeo/sdk": "3.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "eslint": "^9.34.0",
    "globals": "^16.3.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.41.0",
    "vite": "^7.1.3"
  }
}
